import { Container, Stack, Group, Box } from '@mantine/core';

const MainLayoutContent = ({ children }) => {
  return <Box className=" flex-1 p-10">{children}</Box>;
};

export default MainLayoutContent;
